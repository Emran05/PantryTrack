# Flow Audit — confirmed findings (2026-08-07)

Multi-agent audit: 6 flow finders, adversarial verification on every CRITICAL/HIGH.
Refuted findings are excluded. Check items off as they are fixed+verified.

## auth
_Login, signup, Google OAuth and invite-resume are wired correctly and work, but password reset is broken end-to-end (it always dead-ends on "This reset link is invalid or has expired"), and every app _

- [x] **[CRITICAL]** `src/pages/ResetPassword.jsx:24` — Password reset always dead-ends on "This reset link is invalid or has expired" **(FIXED cycle 5)**
  - symptom: A user requests a reset email, clicks the link, and — every single time — lands on a page that says "This reset link is invalid or has expired." with only a "Request a new link" button. Requesting another link produces the same result. There is no wa
  - fix: Capture the recovery signal synchronously at module scope, before supabase-js can wipe the hash, and stop relying on event replay. In src/lib/supabase.js, above createClient: `const hash = typeof window !== 'undefined' ? window.location.hash : ''; export const arrivedViaRecoveryLink = /type=recovery/.test(hash); if (arrivedViaRecoveryLink) { try { sessionStorage.setItem('pw_recovery', '1'); } catc
- [x] **[HIGH]** `src/contexts/AuthContext.jsx:28` — Every return-to-foreground remounts the whole app and blanks in-progress forms
  - symptom: On a phone, the user starts adding an item, switches to Messages to check something, comes back — the screen flashes full-page "Loading home data..." and the Add Item form is empty again. Same on every tab switch, every screen unlock, every notificat
  - fix: Minimal fix, two parts. (1) Preserve user reference identity in src/contexts/AuthContext.jsx onAuthStateChange: replace `setUser(u)` with `setUser(prev => (prev?.id === u?.id ? prev : u))` (same in the getSession().then handler). This alone stops the AuthProvider re-render cascade, the PantryContext refetch, and the full-tree remount, because [user] deps stay referentially stable. (2) Stop the per
  - minor (unverified MEDIUM/LOW): A rejected getSession() strands the user on the loading splash forever; Sign-out leaves the previous account's data on the device, and the nex; A failed or cancelled Google sign-in silently dumps the user on the ma; simplify: the recovery-detection effect is a race against itself; simplify: getSession() duplicates what onAuthStateChange already deliv

## add-item
_Manual add works end to end, and the receipt scan pipeline is well-wired (OCR → text parse → vision fallback → atomic RPC import), but the barcode path is completely dead (the lookup result is always _

- [x] **[CRITICAL]** `src/pages/AddEditItem.jsx:96` — Barcode scan result is always discarded — the feature can never fill in a name **(FIXED cycle 5)**
  - symptom: User taps "Scan Barcode", grants camera access, scans a product. The camera closes and… nothing. The Item Name field stays empty, no "Found: X" toast, no error. The button looks functional but the feature never works, 100% of the time.
  - fix: Track component unmount with a ref instead of the effect-scoped 'aborted' flag for the post-scan lookup. Add at component level: `const mountedRef = useRef(false); useEffect(() => { mountedRef.current = true; return () => { mountedRef.current = false; }; }, []);` (the empty-deps effect with reset-on-mount keeps it correct under StrictMode remounts). Then in the scan-success callback (lines 89-108)
- [x] **[HIGH]** `src/pages/ScanReceipt.jsx:501` — Receipt import button stays enabled during import — a double tap imports every item twice
  - symptom: After reviewing a scanned receipt the user taps "Add 12 items to Pantry". Nothing visibly happens for ~1s (no spinner, button unchanged), so they tap again. Both imports land and the pantry ends up with 24 rows — 12 duplicates the user has to delete 
  - fix: In src/pages/ScanReceipt.jsx: (a) add a reentrancy guard at the top of handleConfirm — `if (!activePantry || isProcessing) return;`; (b) change the submit button to `disabled={selectedCount === 0 || isProcessing}` and swap its label to an in-progress state while importing, e.g. `{isProcessing ? 'Adding…' : `Add ${selectedCount} item${selectedCount !== 1 ? 's' : ''} to Pantry`}` (optionally with th
- [x] **[HIGH]** `src/pages/ScanReceipt.jsx:397` — Zero parsed items leaves the user staring at a photo with no buttons and no message
  - symptom: User photographs a crumpled/blurry receipt (or something that isn't a receipt). OCR + Gemini succeed but return an empty item list. The screen shows just the photo — no "no items found" message, no "Try again", no "Scan Again". The only escape is the
  - fix: In ScanReceipt.jsx runPipeline, handle the empty result explicitly before line 146. After building `enriched`, add: if (enriched.length === 0) { clearPendingScan(); setScanError({ code: 'NO_ITEMS', message: "Couldn't find any grocery items on that photo — try a clearer shot or a different receipt." }); return; } — then fall through to setParsedItems/writePendingScan as today. This reuses the exist
- [x] **[HIGH]** `src/pages/AddEditItem.jsx:63` — Failed load on the Edit screen shows a blank form that silently overwrites the real item
  - symptom: User taps an item to edit it. A network blip makes the load fail; they see a brief "Failed to load item" toast, then a form titled "Edit Item" with everything blank/default. They retype the name and hit "Save Changes" — the item's category, quantity,
  - fix: In the catch block of the load effect (AddEditItem.jsx lines 63-66), mirror the item-not-found path: after showToast('Failed to load item', 'error'), when isEditing navigate('/', { replace: true }) and return (or set an error state that renders a retry screen instead of the form). This prevents the blank Edit form from ever offering a destructive Save Changes against an item whose data never loade
  - minor (unverified MEDIUM/LOW): "Try again" throws away the in-memory photo and forces a full re-shoot; A parsed item missing `name` white-screens the whole app (no error bou; Edit screen hangs on "Loading item details..." forever when no active ; Quantity field can't be cleared, and an intentional 0 silently becomes; simplify: dead `'null'` string branch and a nested duplicate-add pyram

## inventory
_The inventory flow is wired correctly end-to-end (fetch → render → edit/consume/delete → shopping-list round-trip all reach the right Supabase calls), but it is fragile at three seams: the whole authe_

- [x] **[HIGH]** `src/contexts/PantryContext.jsx:57` — Every Supabase token refresh remounts the whole app and wipes in-progress form state
  - symptom: While the user is typing an item into Add Item (or has the Use sheet open), the screen suddenly flashes full-screen "Loading home data..." and everything they typed is gone. Happens roughly hourly with the app open, and on returning to a backgrounded
  - fix: Two-line fix, both halves worth doing. (a) Kill the remount at its consumer: in src/contexts/PantryContext.jsx:57 change the effect deps from `}, [user]);` to `}, [user?.id]);` — pantries only depend on WHO is signed in, not on the token object, so refreshes no longer re-trigger the fetch/loading cycle and App.jsx never swaps in the blocking loader. (b) Make the user state identity-stable at the s
- [x] **[HIGH]** `src/pages/Pantry.jsx:55` — Null activePantry strands Pantry/Dashboard/Shopping on a permanent fake loading spinner
  - symptom: After a network blip at launch (or for a new user whose default-pantry creation fails), the app sits forever on "Loading pantry items..." — no error, no retry, no way forward. Dashboard shows "Loading dashboard..." forever and the Shopping List shows
  - fix: Expose the failure from PantryContext instead of swallowing it: add an `error` state set in the catch (PantryContext.jsx:49-51) and a `retry` function that re-runs fetchPantries, and include both in the provider value. In App.jsx, when `!loading && error`, render an error screen with a Retry button instead of the routes. Additionally, make each stranded page terminate its own loading state when th
- [x] **[HIGH]** `src/components/ItemCard.jsx:102` — Rapid +/- taps silently collapse into a single quantity change (lost update)
  - symptom: User taps "+" three times to go from 2 to 5 eggs; the count lands on 3. Taps appear to do nothing and the inventory count is wrong.
  - fix: Track the pending quantity locally so each tap builds on the last tap, not the last fetch. Minimal change in ItemCard.jsx: add `const pendingQty = useRef(null);`, then in handleQtyChange compute `const base = pendingQty.current ?? item.quantity; const newQty = Math.max(1, base + delta); if (newQty === base) return; pendingQty.current = newQty;` before calling updatePantryItem, and reset `pendingQt
  - minor (unverified MEDIUM/LOW): The quantity +/- buttons have no CSS at all — ~10px tap targets stacke; Delete is instant, unconfirmed and unrecoverable; Decimal quantities cannot be typed into any number input — the value j; No refetch on focus and no fallback if realtime is dead — housemates' ; "Move to Pantry" is a dead button when the rows were already moved els; consumePantryItem is a read-modify-write, so simultaneous housemates l

## notifications
_The flow is correctly wired end to end — VAPID keys match between client and server (verified: same 65-byte P-256 public key, inlined into the built Settings chunk), the SW handles push and notificati_

- [x] **[HIGH]** `src/contexts/AuthContext.jsx:40` — Sign-out never unsubscribes push — the device keeps getting the previous account's expiry reminders
  - symptom: Roommate/shared-device case: A logs out, B logs in on the same phone/laptop. The device keeps showing A's expiry reminders (A's grocery item names on B's lock screen), and B's Settings already reads "Turn off expiry reminders", so B never turns their
  - fix: In AuthContext.jsx signOut(), call disablePushNotifications() BEFORE supabase.auth.signOut() (best-effort, wrapped in try/catch) — the order matters because the push_subscriptions delete needs A's still-valid session to pass RLS: `try { await disablePushNotifications(); } catch {} ; localStorage.removeItem('pantry_active_id'); return supabase.auth.signOut();`. Optionally also harden Settings.jsx: 
- [x] **[HIGH]** `src/lib/push.js:90` — Browser subscribes before the DB write; if the write fails the toggle reads "on" forever and no notification is ever sent
  - symptom: User taps "Turn on expiry reminders", gets "Could not update notification settings", reopens Settings — and the button now says "Turn off expiry reminders". They believe reminders are on. None ever arrive. Tapping the button just says "turned off". T
  - fix: In enablePushNotifications (src/lib/push.js), move the failure-prone server work ahead of, or roll back after, the irreversible browser subscribe: (1) call supabase.auth.getUser() before subscribing so PUSH_NO_USER can't strand a subscription; (2) wrap the store_push_subscription RPC so that on error the just-created subscription is rolled back before rethrowing — e.g. `if (error) { if (createdHer
  - minor (unverified MEDIUM/LOW): Every reminder reuses tag 'pantry-expiry' without renotify — day 2 onw; No pushsubscriptionchange handler — endpoint rotation silently ends re; Notification.requestPermission() runs after two awaits, risking iOS's ; Service-worker registration race produces a false "only works in the i; notificationclick hard-navigates an already-open app, and navigate() r; simplify: the push fallback message is a three-way nested ternary with

## recipes
_The recipes flow is fundamentally wired correctly end to end — local suggestions render instantly, the Gemini user-key → server-proxy fallback works, and cook/add-to-list writes reach Supabase — but i_

- [x] **[HIGH]** `src/pages/Recipes.jsx:16` — `loading` state is set but never rendered — a slow or failed pantry fetch looks identical to an empty pantry
  - symptom: On every visit to Recipes the user sees "No recipe matches — Add items to your pantry to get personalized recipe suggestions" until items arrive (seconds on campus wifi). If `getPantryItems` throws (network drop, RLS hiccup, expired session), that me
  - fix: In src/pages/Recipes.jsx: (a) add a fetch-error state, e.g. `const [loadError, setLoadError] = useState(false)`; set it true in the catch (line 37-40) and clear it at the start of fetchItems. (b) In the recipes-list JSX, branch before the empty state: if `loading`, render a lightweight skeleton/spinner (reuse the ai-loading-banner pattern); else if `loadError`, render an error banner with a "Retry
- [x] **[HIGH]** `src/pages/Recipes.jsx:223` — "Regenerate AI recipes" fails completely silently once any AI result exists
  - symptom: After one successful generation, tapping "Regenerate AI recipes" when the hourly cap is hit (or the proxy is down, or the key is rejected) shows the spinner for a moment and then… nothing. Same list, no banner, no toast, no explanation. The button re
  - fix: Two changes. (1) Make regenerate failures visible when AI results already exist: in the catch of fetchAINow (Recipes.jsx:90-100), if aiRecipes?.length > 0, fire a toast (e.g. showToast(aiError-specific message such as 'AI cooled down — try again in X. Keeping your last suggestions.', 'error')); alternatively change the banner gate at line 223 to `aiError && !aiLoading` and adjust copy for the isUs
- [x] **[HIGH]** `src/lib/recipes.js:164` — `nameMatchesIngredient` returns true for everything when either side normalizes to an empty string — "I cooked this" can decrement the entire pantry
  - symptom: If an AI recipe's `matched` array contains an empty/symbol-only entry, CookThisModal treats EVERY pantry item as an ingredient of that recipe, pre-checks all of them, and the primary button becomes "Use 23 items". One tap decrements (and deletes at z
  - fix: In src/lib/recipes.js, guard nameMatchesIngredient against empty normalized strings: `const normalizedItem = normalize(itemName); const normalizedIngredient = normalize(ingredient); if (!normalizedItem || !normalizedIngredient) return false; return normalizedItem.includes(normalizedIngredient) || normalizedIngredient.includes(normalizedItem);` This makes unmatchable names (digits/symbols/non-Latin
  - minor (unverified MEDIUM/LOW): Zero of the 10 local fallback recipes are vegan — vegan users see an e; "Add N missing to list" has no in-flight guard or visual feedback — do; Any realtime pantry event silently resets the cook sheet's checkboxes ; "I cooked this" can open a sheet with zero rows and a permanently disa; AI recipe payload is only checked with `Array.isArray` — a missing `id; Generic "Tap to retry" banner hides the actionable server message and 

## onboarding
_The happy path is wired correctly end-to-end (Landing → Auth → auto-created "My Home" → Tour → Pantry), but the invite branch silently drops the invite for anyone whose signup requires email confirmat_

- [x] **[CRITICAL]** `src/pages/JoinPantry.jsx:73` — Pending invite token is stored in sessionStorage, so every email-confirmation signup silently loses the invite **(FIXED cycle 5)**
  - symptom: A roommate taps the invite link, taps "Create an account", gets the "we sent a confirmation link" message, clicks the link in their email (which opens a NEW tab / the mail app's in-app browser), and lands in a brand-new empty "My Home". They are neve
  - fix: Two complementary changes: (1) Persist the pending token in localStorage instead of sessionStorage (survives new tabs and tab-close/return in the same browser; keep the consume-before-redeem removal in JoinPantry.jsx:37). (2) Thread the token through the confirmation email itself so even isolated in-app browsers recover: in Auth.jsx signUp, read the pending token and pass options.emailRedirectTo =
- [x] **[HIGH]** `src/contexts/PantryContext.jsx:49` — Any failure loading/creating the first pantry leaves the app on a permanent "Loading pantry items…" spinner with no error and no retry
  - symptom: A user who signs up on flaky campus wifi (or whose default-pantry creation is rejected) lands on a Pantry page that says "Loading pantry items..." forever, a header dropdown permanently stuck on "Loading...", and a Dashboard stuck on "Loading dashboa
  - fix: Add an error state to PantryContext: in the catch at PantryContext.jsx:49, set e.g. `setError(error)` (and expose `error` plus a `retry` callback that re-runs fetchPantries) instead of silently swallowing. Gate in App.jsx: when `!loading && error && pantries.length === 0`, render an error screen with a Retry button wired to that callback. Optionally also retry automatically on the window 'online' 
- [x] **[HIGH]** `src/contexts/AuthContext.jsx:16` — getSession() has no .catch() and no timeout — a rejected or never-settling promise hangs the entire app on the "P Loading…" splash
  - symptom: The user opens the app (or the installed PWA) and sees the green "P / Loading..." splash forever. Nothing renders — not even the marketing Landing page — and there is no error, no retry, and no way out except force-quitting.
  - fix: In src/contexts/AuthContext.jsx, make the initial session check unable to strand the splash: (1) move setLoading(false) into a .finally() and add a .catch() that logs and sets user to null; (2) add a watchdog timeout (~8s) that forces setLoading(false) if getSession() never settles (covers hung initializePromise/fetch), cleared in the effect cleanup. Example: `let done = false; const watchdog = se
- [x] **[HIGH]** `public/sw.js:105` — A failed lazy-route chunk fetch blanks the whole app — no error boundary exists anywhere, and the SW resolves respondWith() to undefined
  - symptom: A user on a weak connection (or offline in the installed PWA) taps "Dashboard"/"Recipes"/"Scan" in the bottom nav and the entire app disappears — a blank white screen, bottom nav and header gone. The only recovery is a manual reload, which offline ju
  - fix: Primary fix (required): add a small class-based ErrorBoundary in src/App.jsx wrapping the Suspense block (or <AppContent/>) whose fallback renders a "Something went wrong — you may be offline" message with a Retry button that resets the boundary state (and optionally calls location.reload()). Secondary hardening: (a) in src/main.jsx add `window.addEventListener('vite:preloadError', ...)` to interc
  - minor (unverified MEDIUM/LOW): Onboarding and preference state is keyed per-device, not per-user, so ; A malformed or truncated invite link shows "check your connection" ins; Privacy policy, EULA and do-not-sell pages exist but nothing in the ap; Landing hero mockups are display:none on every phone but keep running ; simplify: the page-transition overlay stays mounted 2 seconds after na; simplify: the 34-line quick-actions block is duplicated verbatim in th
