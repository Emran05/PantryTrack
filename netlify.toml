# Netlify build + functions config. The SPA fallback lives in public/_redirects
# (function routes like /api/gemini take precedence over the /* rewrite).

[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
